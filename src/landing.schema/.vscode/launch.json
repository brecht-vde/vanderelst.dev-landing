{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "debug",
        "type": "node",
        "request": "launch",
        "runtimeArgs": ["--loader", "ts-node/esm", "-r", "ts-node/register", "-r", "tsconfig-paths/register"],
        "args": ["${workspaceFolder}/program.ts"],
        "cwd": "${workspaceFolder}"
      }
    ]
  }